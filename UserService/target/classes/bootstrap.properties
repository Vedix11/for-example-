# This defines the application's name for the Config Server
spring.application.name=user-service
# This tells Spring Boot to import configuration from a Config Server
spring.config.import=configserver:
# This specifies the URL of your Config Server
spring.cloud.config.uri=http://localhost:8888